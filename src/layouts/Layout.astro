---
import "../styles/global.css";
import { NavBar } from '../components/NavBar.tsx';
import { Footer } from '../components/Footer.jsx'; // <-- 1. Importa el Footer

interface Props {
	title: string;
}
const { title } = Astro.props;
---
<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Observatorio Ambiental" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-background text-foreground">
		
		<NavBar client:load />
		
		<slot />

    <Footer /> {/* <-- 2. Usa el componente aquí */}

	</body>
</html>